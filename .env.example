# Optional for frontend-only builds:
# leave unset to disable backend calls (no auth/profile fetches)
# VITE_BACKEND_BASE_URL=https://api.example.com
# VITE_DISCORD_AUTH_URL=https://api.example.com/auth/discord/login
