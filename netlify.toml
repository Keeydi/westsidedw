[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# Ensure API calls are not cached aggressively by CDN when proxied.
[[headers]]
  for = "/api/*"
  [headers.values]
    Cache-Control = "no-store"
